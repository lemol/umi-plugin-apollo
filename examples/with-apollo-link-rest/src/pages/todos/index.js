import TodoApp from './components/App';

export default function () {
  return (
    <div>
      <p>This example is based on the official <a href="https://github.com/apollographql/apollo-link-state/tree/master/examples/todo">apollo-link-state Todo App example</a>.</p>
      <TodoApp />
    </div>
  );
}
